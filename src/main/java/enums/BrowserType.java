package enums;

/**
 * Enum representing supported browser types for the automation framework.
 * Add more browser types here (e.g., FIREFOX, SAFARI) as the framework scales.
 */
public enum BrowserType {

    CHROME, // Represents Google Chrome browser

    EDGE    // Represents Microsoft Edge browser

}
